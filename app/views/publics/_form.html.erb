<%= form_with(model: public) do |form| %>
  <% if public.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(public.errors.count, "error") %> prohibited this public from being saved:</h2>

      <ul>
        <% public.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
