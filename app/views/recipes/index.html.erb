<section class="recipe-container">
<div>
 <% @recipes.each do |recipe| %>
      <div class= "recipe">
          <div>
            <%= link_to recipe_path(recipe.id), remote: :true do %>
            <h2><%= recipe.name %></h2>
          <% end %>
           <%= button_to "Delete", recipe_path(recipe.id), method: :delete, class: "btn btn-danger" if can? :destroy, recipe%>
        </div>
    <h3><%= recipe.description %></h3>
  </div>
  <% end %>
</div>
</section>
  